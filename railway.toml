# application.toml

[services.web]
image = "node:18"

[services.web.build]
# Install dependencies
pre_commands = ["yarn install --trace-warnings"]
# Run migrations before the build
commands = ["yarn medusa migrations run", "yarn run start --trace-warnings"]

[services.web.start]
command = "yarn run build"
